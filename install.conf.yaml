- defaults:
        link:
                relink: true

- clean: ['~']

- link:
        ~/.tmux.conf: 
        ~/.vim: 
        ~/.vimrc:
          relink: true

- create:
        - ~/.vim/undo-history
        - ~/playground

- shell:
        - [git submodule update --init --recursive, Installing submodules]
